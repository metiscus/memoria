(load "./libmemoria_script.so")
(define hour 0)

(define (time-desc time)
    (cond ((< time 11) 'morning)
          ((< time 18 ) 'afternoon)
          (else 'evening)
))

(define (display-all . vs)
  (for-each display vs))

(define (derp)
    (display "Running: test.scm\n")
    (random_init 1000)
    (display-all "here is a random number it should be 3: " (random_uint 5) "\n")
    (sleep 100000)
    (random_reseed_time)
    (display-all "here is a random number: " (random_uint 5) "\n")
    (display (time-desc hour))
    (display "\n")
    (define player (player_new))
    (player_move player 1 1)
    (display-all "<" (player-x player) ", " (player-y player) ">\n")
    (player_delete player)
    (display "Test Done!\n")
)
